# Session 3 artifacts

This folder is reserved for Session 3 outputs.

## Expected subfolders
- `runs/` — final participant artifacts for each condition
- `scoring/` — scored evaluations after all runs are complete

## Timing rule
Use wall-clock start → final artifact handoff as the primary timing metric.
Optionally estimate active agent-minutes separately.

## Contamination rule
Do not place answer-key material in `runs/`.
Scoring files should be created only after all three condition outputs are submitted.
